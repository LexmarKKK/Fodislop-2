#nullable enable

using Fodinae.UI.HUD.Inventory.View;
using UnityEngine;

namespace Fodinae.UI.HUD.Inventory.Presenter
{
    [RequireComponent(typeof(InventoryView))]
    public class InventoryPresenter : MonoBehaviour
    {
    }
}
